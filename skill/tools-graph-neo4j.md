# Tools: Graph Neo4j

Exact Python argument names (MCP tools):

```
find_paths(from_entry_id, to_entry_id, max_depth=5)   # max_depth capped at 8
find_pattern(description, limit=20)                    # limit capped at 50
suggest_related(entry_id, limit=10)
graph_stats()
```
