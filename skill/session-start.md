# Session Start

How to send these calls (SSE, Bearer token, `tools/call`, `arguments` vs client `args`): **`tool-invocation.md`**.

```
1. get_global_context()
2. get_structure(depth=2)
3. search(query)
```
